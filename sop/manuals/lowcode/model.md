# 数据模型

## 功能说明

数据模型是低代码开发的基础，每个模型对应数据库中的一张表。通过配置模型和字段，可以快速生成业务功能。

## 创建模型

1. 进入「在线开发」→「数据模型」。
2. 点击「+ 新增模型」按钮。
3. 输入模型编码（英文，例如 `customer`）。
4. 输入模型名称（中文，例如「客户」）。
5. 表名可留空，系统会自动生成 `lc_编码`。
6. 点击「确定」保存。

> 保存后系统会自动在数据库中创建对应的物理表。

## 模型管理

模型列表中提供以下操作：

- **设计**：进入模型设计器，配置字段、表单、列表
- **运行**：打开自动生成的业务页面
- **编辑**：修改模型名称和描述
- **删除**：删除模型及其物理表

## 删除模型

删除模型会同时删除对应的物理表和所有数据，请谨慎操作。
