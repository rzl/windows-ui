# 子线：数据审计日志

## 来源阶段

[phase-10：应用发布与版本管理 + 数据审计日志](../../by-phase/phase-10.md)

## 目标

对启用审计的模型自动记录字段级数据变更历史。

## 关键实现

- `data_audit_logs` 表
- 模型可配置 `enable_audit`
- 自动记录创建、更新、删除、批量删除、导入操作
- 记录变更前后字段对比（diff）

## 验收标准

- [x] 为模型启用审计后，操作自动记录字段级变更
- [x] 可按模型/记录/操作人/时间筛选查询
