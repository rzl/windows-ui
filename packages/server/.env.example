PORT=3000
NODE_ENV=development

# JWT 配置
JWT_SECRET=windows-ui-lowcode-secret-key-change-in-production
JWT_ACCESS_EXPIRES=2h
JWT_REFRESH_EXPIRES=7d

# 数据库配置（默认 SQLite）
DB_CLIENT=sqlite3
DB_FILENAME=./data/lowcode.sqlite
